describe("App", () => {
    it('renders the initial view', () => {});

    it('passes value from searchInput to the API', () => {});

    it('sets app state to the results from the API', () => {});

    it('renders forecast and map components if good response from API', () => {});

    it('does not render Map if no address returned from API', () => {});

    it('does not render Forecast if no forecast object returned from API', () => {});
});
