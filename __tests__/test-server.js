describe("Server", () => {
    it('renders the initial view', () => {

    });

    it('throws an error if no API keys set in env', () => {});

    it('builds a valid url for the forecast API', () => {});

    it('responds to requests at /api/forecast/:addressString', () => {});

    it('builds the proper forecast response object', () => {});

    it('passes useful error information to the client', () => {});

    it('returns an error for requests to unauthoried url paths', () => {});
});
